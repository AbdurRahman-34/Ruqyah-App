const wrapper = document.querySelector(".wrapper"),
musicImg = wrapper.document.querySelector(".img-area img"),
musicName = wrapper.document.querySelector("song-details .name"),
musicArtist = wrapper.document.querySelector("song-details .artist");


let musicIndex = 2;
window.addEventListener("load", () => {
    loadMusic(musicIndex);  // calling load Function once windows loaded
})


// Load Music Function :::::
const loadMusic = () {

}